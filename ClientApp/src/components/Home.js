import React, { Component } from 'react';

export class Home extends Component {
  static displayName = Home.name;

  render () {
    return (
      <div>
            <h1>React with ASP.NET</h1>
            <p>This is a simple React project utilizing an Asp.Net backend, routing, and react drag and drop features. </p>
      
      </div>
    );
  }
}
